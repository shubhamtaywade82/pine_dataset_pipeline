# Overlay for Pine Script v6 reference entries (merged after automated extraction).
# Use for symbols that HTML parsing misses or to pin canonical signatures.
functions:
  ta.rsi:
    signature: "ta.rsi(series float source, simple int length) → series float"
    description: "Relative Strength Index. Seed entry; prefer extracted docs when available."
namespaces: {}
